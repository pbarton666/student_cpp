// Bare-bones - this gets names and memory requirements on the table

class IntRoutines{
    public:
        //IntRoutines();          // if defining a constructor
        int addInts(int, int);    // class method
    private:
        int class_member;         // class member (attribute)
};