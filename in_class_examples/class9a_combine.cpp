// include our header file
#include "class9a_combine.hpp" 

// Specify the already-declared class method
int IntRoutines::addInts(int a, int b){
    return a + b;
}

// We don't need a main() function here because this not an 
//  entry point.