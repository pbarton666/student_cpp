command pallette F1

Application menu (stacked bars on left) 
.. Teminal .. New Terminal
    .. Terminal .. Configure Default Build Task  (cntl-shift-b)

.. Terminal .. Run Active File

Run .. without debugging .. Select Environment .. (pick C++ GDB/LLDB)  (creates launch.json)
the program we wint to debug is  "${fileDirname}\\${fileBasenameNoExtension}"   // no .exe



Exclude the .vscode dir from git repo
